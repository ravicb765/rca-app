# Backstage App (placeholder)

This folder contains the Backstage frontend application. Use `npx @backstage/create-app@latest` to scaffold a full app.

For local development, install dependencies and run `yarn start` in the `packages/app` folder.
