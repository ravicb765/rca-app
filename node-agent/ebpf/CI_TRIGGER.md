# CI trigger

This file is a harmless, small change whose only purpose is to trigger the path-limited eBPF CI workflow to exercise the `verify-load` job on a privileged self-hosted runner.

Do not modify for functional reasons; remove after validation.
