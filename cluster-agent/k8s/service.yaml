apiVersion: v1
kind: Service
metadata:
  name: cluster-agent
  namespace: observability
  labels:
    app: cluster-agent
spec:
  selector:
    app: cluster-agent
  ports:
    - protocol: TCP
      port: 9100
      targetPort: 9100
