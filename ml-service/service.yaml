apiVersion: v1
kind: Service
metadata:
  name: ml-service
  namespace: observability
  labels:
    app: ml-service
spec:
  selector:
    app: ml-service
  ports:
  - port: 5000
    targetPort: 5000
    name: http